#include "emmy.h"

/**
 * main - Entry point
 *
 * Return: 0
 */

int main(void)
{
	char format[1024];

	while (1)
	{
		emmyf(format);

	/**
	 * display_prompt();
	 * read_command(command, sizeof(command));
	 * execute_command(command);
	 */

	}
	return (0);
}
